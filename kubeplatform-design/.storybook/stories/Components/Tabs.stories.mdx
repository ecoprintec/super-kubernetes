import { useState } from "react";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Tabs from "components/Tabs";

<Meta title="Components/Tabs" component={Tabs} />

# Tabs

## Props

<Props of={Tabs} />

## Examples

### Basic

Basic usages for `Tabs` component.

<Preview>
  <Story name="basic tabs">
    {() => {
      const [tab, setTab] = useState("1");
      const { TabPanel } = Tabs;
      return (
        <div style={{ backgroundColor: "#1c1e20", padding: "100px" }}>
          <Tabs activeName={tab} onChange={(newTab) => setTab(newTab)}>
            <TabPanel label="Overview" name="1" icon="cluster">
              Overview
            </TabPanel>
            <TabPanel label="Pods" name="2" icon="project">
              Pods
            </TabPanel>
            <TabPanel label="Deployments" name="3" icon="project">
              Deployments
            </TabPanel>
            <TabPanel label="StatefulSets" name="4" icon="project">
              StatefulSets
            </TabPanel>
          </Tabs>
          <Tabs
            type="button"
            activeName={tab}
            onChange={(newTab) => setTab(newTab)}
          >
            <TabPanel label="Overview" name="1" icon="cluster">
              Overview
            </TabPanel>
            <TabPanel label="Pods" name="2" icon="project">
              Pods
            </TabPanel>
            <TabPanel label="Deployments" name="3" icon="project">
              Deployments
            </TabPanel>
            <TabPanel label="StatefulSets" name="4" icon="project">
              StatefulSets
            </TabPanel>
          </Tabs>
          <Tabs
            direction="vertical"
            activeName={tab}
            onChange={(newTab) => setTab(newTab)}
          >
            <TabPanel label="Overview" name="1" icon="cluster">
              Overview
            </TabPanel>
            <TabPanel label="Pods" name="2" icon="project">
              Pods
            </TabPanel>
            <TabPanel label="Deployments" name="3" icon="project">
              Deployments
            </TabPanel>
            <TabPanel label="StatefulSets" name="4" icon="project">
              StatefulSets
            </TabPanel>            
          </Tabs>
        </div>
      );
    }}
  </Story>
</Preview>
